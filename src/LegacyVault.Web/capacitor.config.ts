import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.legacyvault.app',
  appName: 'Legacy Vault',
  webDir: 'dist',
  android: {
    backgroundColor: '#13131f',
  },
};

export default config;
