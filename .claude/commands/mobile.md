Start the LegacyVault Mobile Expo dev server.

Run from `src/LegacyVault.Mobile/`:
```
npx expo start
```

If the user passed arguments (e.g. `--ios`, `--android`, `--web`, `--tunnel`), append them: $ARGUMENTS

Report the QR code URL or dev server address, and any startup warnings or errors.
