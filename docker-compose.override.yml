# Development overrides — automatically merged with docker-compose.yml
# Exposes the API port directly for Swagger access during development
services:
  api:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "5500:5500"  # Swagger UI at http://localhost:5500/swagger
